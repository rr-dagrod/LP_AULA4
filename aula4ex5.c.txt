#include <stdio.h>
    int main() {
        int n;
        printf("Insira um número inteiro de 0 a 9\n");
        scanf("%d", &n);
        while (n < 11) {
            printf("%d\n", n);
            n = n + 1;
        }
        return 0;
    }