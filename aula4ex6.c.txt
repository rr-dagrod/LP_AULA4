#include <stdio.h>
    int main() {
        int contagem;
        printf("contagem regressiva\n");
        contagem = 5;
        while(contagem >= 0) {
            printf("%d\n", contagem);
            contagem = contagem - 1;
        }
        printf("lançar");
        return 0;
    }